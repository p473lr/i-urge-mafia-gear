#include <stdio.h>#include <stdlib.h>#define TRUE 1#define FALSE 0int main() {	freopen("bacon.in","r",stdin); //open the file	char buffer[100];	char* france = "France";	fgets(buffer,100,stdin);	int i,j,k,n,count,valid;	sscanf(buffer,"%d", &n);	for(i=1; i<=n; i++) {		for(j=0; j<100; j++) //clear the buffer			buffer[j] = 0;		fgets(buffer,100,stdin);		count=0;		for(j=0; j<90; j++) { //choose each start			valid = TRUE;			for(k=0; k<6; k++) //consider each letter in France				if(buffer[j+k] != france[k])					valid = FALSE;			if(valid) //this word was France				count++;		}		printf("Sentence #%d: %d\n", i, count);	}	fclose(stdin);	return 0;}